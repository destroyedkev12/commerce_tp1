<p id="notice"><%= notice %></p>

<h1>Clients Has Conjoints</h1>

<table>
  <thead>
    <tr>
      <th>Clients</th>
      <th>Clients conjoint</th>
      <th>Datedebut</th>
      <th>Datefin</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @clients_has_conjoints.each do |clients_has_conjoint| %>
      <tr>
        <td><%= clients_has_conjoint.Clients_id %></td>
        <td><%= clients_has_conjoint.Clients_Conjoint_id %></td>
        <td><%= clients_has_conjoint.DateDebut %></td>
        <td><%= clients_has_conjoint.DateFin %></td>
        <td><%= link_to 'Show', clients_has_conjoint %></td>
        <td><%= link_to 'Edit', edit_clients_has_conjoint_path(clients_has_conjoint) %></td>
        <td><%= link_to 'Destroy', clients_has_conjoint, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Clients Has Conjoint', new_clients_has_conjoint_path %>
