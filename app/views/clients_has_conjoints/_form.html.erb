<%= form_with(model: clients_has_conjoint, local: true) do |form| %>
  <% if clients_has_conjoint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(clients_has_conjoint.errors.count, "error") %> prohibited this clients_has_conjoint from being saved:</h2>

      <ul>
      <% clients_has_conjoint.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Clients_id %>
    <%= form.number_field :Clients_id, id: :clients_has_conjoint_Clients_id %>
  </div>

  <div class="field">
    <%= form.label :Clients_Conjoint_id %>
    <%= form.number_field :Clients_Conjoint_id, id: :clients_has_conjoint_Clients_Conjoint_id %>
  </div>

  <div class="field">
    <%= form.label :DateDebut %>
    <%= form.date_select :DateDebut, id: :clients_has_conjoint_DateDebut %>
  </div>

  <div class="field">
    <%= form.label :DateFin %>
    <%= form.date_select :DateFin, id: :clients_has_conjoint_DateFin %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
