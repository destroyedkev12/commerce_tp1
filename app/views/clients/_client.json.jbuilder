json.extract! client, :id, :id, :Prenom, :Nom, :DateNaissance, :NAS, :adresses_id, :NombreEnfants, :CompteTaxesProprietaire, :integer, :created_at, :updated_at
json.url client_url(client, format: :json)
